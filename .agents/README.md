# Storefront agent skills

This directory contains Storefront-local skill copies. `.agent/skills` and `.agent/rules` are canonical; no skill is loaded from the parent workspace or Admin/API repositories.
